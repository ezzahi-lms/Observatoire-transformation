agent:
  name: Observatoire Transformation
  version: 1.1.0
  default_sector: pharma_maroc
collection:
  days_back: 35
  max_search_results: 15
  max_articles_total: 60
analysis:
  provider: gemini                  # anthropic | gemini
  model: claude-sonnet-4-6          # Modèle Anthropic (si provider=anthropic)
  gemini_model: gemini-2.5-flash    # Modèle Gemini  (si provider=gemini — free tier)
  gemini_max_tokens: 4096           # Limite sortie Gemini (free tier plus restrictif)
  gemini_max_articles: 12           # Nb articles max envoyés à Gemini (prompt plus court)
  gemini_summary_len: 150           # Longueur résumé article pour Gemini (vs 400 Anthropic)
  max_tokens: 8192
  temperature: 0.3
reporting:
  formats:
  - docx
  - html
  output_dir: reports
  filename_pattern: veille_{sector}_{date}
scheduling:
  enabled: true
  frequency: monthly
  day_of_month: 1
  hour: 8
  minute: 0
logging:
  level: INFO
  file: reports/agent.log
