# API Anthropic (provider=anthropic)
anthropic>=0.40.0

# API Google Gemini (provider=gemini — free tier)
google-genai>=1.0.0

# API Groq (provider=groq — free tier, llama-3.3-70b)
groq>=0.9.0

# Collecte des données
ddgs>=1.0.0
feedparser>=6.0.11
requests>=2.32.0

# Génération des rapports
python-docx>=1.1.2
jinja2>=3.1.4
python-pptx>=1.0.0

# Interface graphique
streamlit>=1.28.0
streamlit-authenticator>=0.4.0

# Scheduler
apscheduler>=3.10.4

# Config & environnement
python-dotenv>=1.0.1
pyyaml>=6.0.2

# PDF (optionnel — décommenter si souhaité)
# weasyprint>=62.0
